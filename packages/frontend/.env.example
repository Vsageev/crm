# Override the backend URL used by Vite proxy (auto-detected from certs by default)
# VITE_API_URL=https://localhost:3847

# Enables the frontend devtools widget for local feature flag and variant switching.
VITE_DEVTOOLS_WIDGET_ENABLED=false
