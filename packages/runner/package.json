{
  "name": "openwork-runner",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "openwork-runner": "dist/index.js"
  },
  "scripts": {
    "dev": "OPENWORK_RUNNER_WORKSPACE_ROOT=${OPENWORK_RUNNER_WORKSPACE_ROOT:-$(cd ../.. && pwd)} tsx src/index.ts",
    "build": "tsc",
    "prestart": "pnpm --filter shared build && pnpm run build",
    "start": "OPENWORK_RUNNER_WORKSPACE_ROOT=$PWD node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "shared": "workspace:*",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
